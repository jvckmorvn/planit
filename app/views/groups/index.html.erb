<h1>My Groups</h1>

<div class="container">
  <div class="row">
    <% @groups.each do |group| %>
      <%= render "group_card", group: group %>
    <% end %>
    <%= render "form", group: @group %>
  </div>
</div>
