
<div class="group-channel-event d-flex justify-content-between px-2">
  <div class="d-flex align-items-center">
    <div class="me-1" style="background-color: <%= event.colour %>;"></div>
    <div><%= event.name %></div>
    <div class="ms-2"><%= event.locked ? "🔒" : "" %></div>
  </div>
  <div class="d-flex align-items-center">
    <div><%= cl_image_tag event.user.avatar.key, class: "avatar-small" %></div>
  </div>
  <%= link_to raw("<i class='fa-solid fa-xmark'></i>"), group_event_path(event), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "event-card-delete-btn" %>
</div>
