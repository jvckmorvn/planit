
<div class="group-channel-event" data-select-event-target="eventCard" data-action="click->select-event#selectEvent" data-event-colour=<%= event.colour %>>
  <div class="d-flex align-items-center">
    <div class="me-1" style="width: 12px; height: 12px; border-radius: 50%; background-color: <%= event.colour %>;"></div>
    <div class="ms-2"><%= event.name %></div>
    <div class="ms-2"><%= event.locked ? "🔒" : "" %></div>
  </div>
  <div class="d-flex align-items-center group-channel-event-row-avatar">
    <div><%= cl_image_tag event.user.avatar.key, class: "avatar-small" %></div>
  </div>
  <%= link_to raw("<i class='fa-solid fa-xmark'></i>"), group_event_path(event), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "event-card-delete-btn" %>
</div>
