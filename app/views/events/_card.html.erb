<div class="group-channel-event">
  <div class="d-flex align-items-center">
    <div class="me-1" style="background-color: <%= event.colour %>;"></div>
    <div><%= event.name %></div>
    <div class="ms-1"><%= event.locked ? "🔒" : "" %></div>
    <div class="avatar-in-event"><%= cl_image_tag event.user.avatar.key, class: "avatar-small" %></div></div>
</div>
